#![allow(non_upper_case_globals)]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]

use vx::*;

fn main() {
    //
    unsafe {
        initVulkan();
    }

    // buffer creation

    // descriptor

    // pipeline generation

    // buffer submit
}
